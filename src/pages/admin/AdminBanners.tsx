import AdminStubPage from "./AdminStubPage";
import { Image } from "lucide-react";
export default function AdminBanners() {
  return <AdminStubPage title="Promo Banners" description="Homepage and dashboard promotional banners." icon={Image} features={["Scheduled banners", "Audience targeting", "Click tracking"]} />;
}
